{% set pack = pack|default(exam is defined ? getPauBundlePackForExam(exam) : null) %}
{% if pack %}
  {% include 'common/products/pau_bundle_pack_promo.html.twig' with {
    pack: pack,
    compact: compact|default(false),
    event_label: event_label|default('contextual')
  } only %}
{% endif %}
