<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for v in videos %}
<url>
  <loc>{{ url('youtube_video', { 'videoSlug' : v.slug })}}</loc>
  <lastmod>{{ v.updatedAt|date('c')}}</lastmod>
  <changefreq>monthly</changefreq>
</url>
{% endfor %}
</urlset>