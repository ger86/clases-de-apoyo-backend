karser_recaptcha3:
    enabled: '%env(bool:RECAPTCHA3_ENABLED)%'
    site_key: '%env(RECAPTCHA3_KEY)%'
    secret_key: '%env(RECAPTCHA3_SECRET)%'
    score_threshold: 0.5

when@test:
    karser_recaptcha3:
        enabled: false
